// firestore.rules (tightened for sectors + vault + logs)
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
<<<<<<< HEAD
    function isAuthed() { return request.auth != null; }
    function isOwner(ownerId) { return isAuthed() && request.auth.uid == ownerId; }

    match /vaults/{vaultId} {
      allow read, write: if isOwner(resource.data.ownerId);
    }
    match /vault_audit/{logId} {
      allow create: if isAuthed();
      allow read: if isOwner(resource.data.ownerId);
    }
    match /sectors/{sector} {
      match /notebook_pages/{doc} {
        allow read: if isAuthed();
        allow create, update: if isAuthed();
      }
      match /items/{doc} {
        allow read: if isAuthed();
        allow create, update: if isAuthed();
      }
    }
    match /deployment_logs/{doc} {
      allow read, create: if isAuthed();
=======
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth.uid == userId;
>>>>>>> c0f641d (🪓 Codex restoration: 147 changes swept and secured post-quota breach)
    }
  }
}
